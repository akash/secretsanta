<% if @party.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@party.errors.count, "error") %> prohibited this Party from being saved:</h3>

      <ul>
        <% @party.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>

<%= form_for [:admin, @party] do |form| %>
  <div class="field">
    <%= form.label :name %>
  </div>
  <div class="field">
    <%= form.text_field :name %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>

<%end %>
